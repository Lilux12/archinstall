# Arch Linux Automated Installer v2.0

Профессиональный автоматический установщик Arch Linux на Python 3 с интерактивным TUI интерфейсом через pythondialog.

## Возможности

- 🎨 **Интерактивный TUI интерфейс** - красивое меню на основе dialog
- 🌍 **Мультиязычность** - поддержка русского и английского языков
- 💾 **Автоматическая разметка диска** - ext4, btrfs с subvolumes
- 🎮 **Автоопределение видеокарты** - NVIDIA, AMD, Intel, гибридная
- 🖥️ **Поддержка всех DE/WM** - KDE, GNOME, XFCE, i3, Sway и другие
- ⌨️ **Гибкая настройка раскладок** - поддержка множества языков
- 📦 **Профили установки** - Desktop, Minimal, Server, Xorg
- 🔐 **Управление пользователями** - создание root и обычного пользователя
- 🔄 **AUR помощники** - yay и paru
- 📝 **Подробное логирование** - все операции записываются в /var/log/archinstall.log
- 💾 **Сохранение/загрузка конфиг** - повторяемые установки
- ⚡ **UEFI и BIOS** - поддержка обоих режимов загрузки

## Требования

### Системные требования
- Arch Linux Live ISO
- Минимум 512 MB RAM
- Подключение к интернету
- Запуск от root пользователя

### Зависимости Python

```bash
pip install pythondialog psutil requests pyyaml
```

### Системные утилиты (обычно уже присутствуют в Live ISO)
- `lsblk` - информация о дисках
- `parted`, `sgdisk` - управление разделами
- `mkfs.*` - форматирование
- `pacstrap`, `arch-chroot` - установка
- `reflector` - выбор зеркал
- `lspci`, `lsusb` - определение железа

## Установка

### На Arch Linux Live ISO

```bash
# Установить зависимости
pacman -Sy python python-pip dialog

# Клонировать репозиторий
git clone https://github.com/user/arch-installer.git
cd arch-installer

# Установить Python зависимости
pip install -r requirements.txt

# Запустить установщик
sudo python main.py
```

### Или прямо из интернета

```bash
# Загрузить и запустить одной командой
curl -L https://raw.githubusercontent.com/user/arch-installer/main/main.py | sudo python3
```

## Использование

### Простое использование

```bash
sudo python main.py
```

### С выбором языка

```bash
# Русский интерфейс (по умолчанию)
sudo python main.py --lang ru

# Английский интерфейс
sudo python main.py --lang en
```

### С конфигурацией

```bash
# Загрузить сохраненную конфигурацию
sudo python main.py --config my_config.json

# Загрузить и запустить в автоматическом режиме
sudo python main.py --config my_config.json --auto
```

## Архитектура

```
arch-autoinstaller/
├── main.py                    # Точка входа, главное меню
├── config.py                  # Конфигурация и i18n
├── installer/
│   ├── disk.py               # Управление дисками
│   ├── network.py            # Настройка сети
│   ├── packages.py           # Пакеты и профили
│   ├── graphics.py           # Видеодрайверы
│   ├── desktop.py            # Desktop Environment
│   ├── bootloader.py         # GRUB/systemd-boot
│   ├── localization.py       # Локали, раскладки, timezone
│   └── users.py              # Пользователи и группы
├── ui/
│   ├── dialogs.py            # Обертки для dialog
│   └── progress.py           # Прогресс-бары
├── utils/
│   ├── logger.py             # Логирование
│   ├── validators.py         # Валидация
│   ├── system.py             # Системные проверки
│   └── executor.py           # Выполнение команд
└── configs/
    ├── packages.json         # Списки пакетов
    └── profiles.json         # Профили установки
```

## Поддерживаемые Desktop Environments

- **KDE Plasma** - современное полнофункциональное окружение
- **GNOME** - элегантный и простой интерфейс
- **XFCE** - легковесное и быстрое
- **Cinnamon** - традиционный рабочий стол
- **MATE** - классическое окружение
- **i3wm** - тайловый менеджер окон
- **Sway** - Wayland тайловый композитор
- **Команда строка** - серверная минимальная установка

## Поддерживаемые видеокарты и драйверы

- **NVIDIA**
  - Proprietary drivers (лучшая производительность)
  - Nouveau open-source (стабильные)
  - Hybrid (для ноутбуков)
- **AMD**
  - Open-source drivers (полная поддержка)
  - AMDGPU
- **Intel**
  - Open-source drivers (встроенная графика)
- **Generic** - базовые драйверы

## Профили установки

### Desktop
Полная установка для рабочей станции с браузером, офисными приложениями и инструментами разработки.

### Minimal
Минимальная система - только необходимое для работы.

### Server
Серверная конфигурация с SSH, системными утилитами, без GUI.

### Xorg
Базовая графика без Desktop Environment - для кастомных конфигураций.

## Функции

### Управление дисками
- Автоопределение всех доступных дисков
- Выбор диска для установки
- Автоматическая разметка:
  - ext4 (простой и надежный)
  - btrfs (с поддержкой subvolumes)
- Ручная разметка через cfdisk
- Поддержка UEFI и BIOS

### Локализация
- Выбор языка интерфейса (русский, английский)
- Поиск и выбор раскладок клавиатуры
- Переключение между несколькими раскладками
- Автоматическая настройка locale
- Выбор часового пояса
- Поддержка multilib (32-битные библиотеки)

### Сеть и пользователи
- Установка hostname
- Выбор сетевого менеджера (NetworkManager, systemd-networkd, iwd)
- Создание root пользователя
- Создание обычного пользователя
- Управление группами пользователя
- Автоматическая настройка sudo

### Загрузчик
- Автоопределение UEFI/BIOS
- GRUB (универсальный)
- systemd-boot (легкий, для UEFI)

### Дополнительно
- Автоматическое выбор зеркал через reflector
- Установка AUR helper (yay или paru)
- Выбор дополнительных пакетов по категориям
- Подробное логирование всех операций
- Сохранение и загрузка конфигурации

## Логирование

Все операции логируются в `/var/log/archinstall.log`:

```bash
# Просмотр логов в реальном времени
tail -f /var/log/archinstall.log

# Полный просмотр
cat /var/log/archinstall.log
```

## Примеры

### Пример 1: Установка KDE Plasma на UEFI с NVIDIA

```
1. Выбрать диск: /dev/sda
2. Схема разметки: Автоматическая (ext4)
3. Видеокарта: NVIDIA (proprietary)
4. Desktop: KDE Plasma
5. Раскладки: Russian, US English
6. Профиль: Desktop
7. Hostname: my-desktop
8. Пользователь: user
9. Timezone: Europe/Moscow
10. Локали: ru_RU.UTF-8, en_US.UTF-8
```

### Пример 2: Минимальная серверная установка

```
1. Диск: /dev/sda
2. Разметка: auto_ext4
3. GPU: generic
4. Desktop: Нет
5. Профиль: Server
6. Hostname: myserver
7. Сеть: systemd-networkd
8. Пользователь: admin
9. Timezone: UTC
```

## Безопасность

- ⚠️ **ВНИМАНИЕ**: Установщик форматирует диск! Все данные будут удалены!
- Пароли хранятся только в памяти (не записываются на диск)
- Все команды логируются в файл логов
- Валидация всех пользовательских входных данных
- Проверка прав доступа перед установкой

## Особенности

### Новое в v2.0

- ✨ Переписан на Python 3 с использованием pythondialog
- 🎨 Улучшенный TUI интерфейс
- 🌍 Полная поддержка русского языка
- 📊 Интерактивный прогресс-бар установки
- 💾 Сохранение и загрузка конфигурации
- 🔧 Модульная архитектура для расширения
- 📝 Подробное логирование
- 🚀 Оптимизированная установка пакетов

## Поддержка и помощь

### Решение проблем

Если установка завершилась с ошибкой, посмотрите логи:

```bash
tail -100 /var/log/archinstall.log
```

### Частые ошибки

**Ошибка: "Нет подключения к интернету"**
- Проверьте подключение: `ping archlinux.org`
- Настройте сеть: `nmtui` или `ip link set <iface> up`

**Ошибка: "This script must be run as root"**
- Запустите установщик с sudo: `sudo python main.py`

**Ошибка: "Dialog not installed"**
- Установите dialog: `pacman -S dialog`

## Лицензия

GNU General Public License v3.0

## Автор

Arch Community

## Благодарности

- Arch Linux Team за отличный дистрибутив
- pythondialog за TUI библиотеку
- psutil за системную информацию

## Связь

- Основное хранилище: https://github.com/user/arch-installer
- Вопросы и предложения: GitHub Issues
- Полные примеры: https://github.com/user/arch-installer/wiki
